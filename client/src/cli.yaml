name: Gib Package Manager
version: "0.1.0"
author: Nathan Hare <me@laspruca.nz>
about: CLI for the gib package manager
args:
subcommands:
  - install:
      about: Installs given package
      requires: INPUT
      args:
        - INPUT:
            help: The traget package
  - add_repo:
      about: Adds a given repositery
      requires: INPUT
      args:
        - INPUT:
            help: Address of the repositery